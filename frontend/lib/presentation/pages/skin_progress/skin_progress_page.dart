import 'package:flutter/material.dart';

class SkinProgressPage extends StatelessWidget {
  const SkinProgressPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}