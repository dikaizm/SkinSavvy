import 'package:flutter/material.dart';

class AppTheme {
  // App colors
  static const Color primaryColor = Color(0xFFED706B);
  static const Color secondaryColor = Color(0xFFFFC472);
  static const Color backgroundColor = Color(0xFFF8FAFC);

  // Colors
  static const Color neutralColor100 = Color(0xFFF3F4F6);
  static const Color neutralColor300 = Color(0xFFD1D5DB);

  static const Color orangeColor = Color(0xFFFF9A70);

  static const Color primary50 = Color(0xFFFEEEE3);
  static const Color linen = Color(0xFFFEEEE3);
  static const Color dartTeal = Color(0xFF358098);

  // Box shadows
  static const BoxShadow boxShadow = BoxShadow(
    color: Color.fromRGBO(0, 0, 0, 0.08),
    blurRadius: 16,
    offset: Offset(0, 2),
  );
}
